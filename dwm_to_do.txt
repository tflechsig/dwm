DWM To Do List
x - Focus window on mouse click, not on mouse hover
x - Add alt-tab and alt-shift-tab as a method for switching windows
x - Add window count along with the window number to both the deck and monocle
    layouts
x - Add  clickable icon to the left side of the status bar to launch rofi
x - Modify the current tag indicator
- Add status to the right side of the bar
  x - Fix status button clickable width
  - Time status won't update properly if another status button is clicked
    simultaneously
- Cycle through all layouts when clicking the layout button
- Change square that indicates a window is open in the workspace to a different
  color or workspace text
- Use color in status barsquare that indicates a window is open in the workspace
  to a different color or workspace text
